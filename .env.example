# Optional for MVP — without this, the app uses demo portfolio data
DATABASE_URL="postgresql://user:password@localhost:5432/wealthspace?schema=public"

# Optional — required only for live Weekly AI Advisor responses
OPENAI_API_KEY="sk-..."
